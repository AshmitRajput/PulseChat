# Web framework
fastapi>=0.115.0
starlette>=0.48.0
uvicorn[standard]>=0.32.0

# Validation
pydantic>=2.12.0
pydantic_core>=2.41.0
pydantic-settings>=2.6.0

# Database - async stack (Phase 1)
SQLAlchemy>=2.0.36
asyncpg>=0.30.0
# psycopg kept only if you still need a sync connection anywhere (e.g. Alembic
# migrations later); safe to drop if nothing else uses it.
psycopg[binary]>=3.2.3

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.2.0
cryptography>=43.0.3
python-multipart>=0.0.12

# Cache / pub-sub (Phase 1)
redis[hiredis]>=5.2.0

# Utilities
python-dotenv>=1.0.1
PyYAML>=6.0.2
websockets>=13.1
